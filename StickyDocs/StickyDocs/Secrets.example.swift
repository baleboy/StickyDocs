// Template for Secrets.swift (which is gitignored).
// Copy this file to Secrets.swift and fill in the values from your
// Google Cloud Console -> Credentials -> OAuth 2.0 Client ID (Desktop app).
//
// NOTE: This file's contents are commented out so it can be safely
// committed without producing a duplicate `Secrets` enum.

/*
import Foundation

enum Secrets {
    static let googleClientID = "YOUR_CLIENT_ID.apps.googleusercontent.com"
    static let googleClientSecret = "YOUR_CLIENT_SECRET"
}
*/
